let val;

// Number to string
val = String(512);
val = String(10 + 12);

// Boolean to string
val = String(true);

// Date to a string
val = String(new Date());

console.log(val);
console.log(typeof val);
console.log(val.length);